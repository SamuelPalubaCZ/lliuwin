# Ubuntu image build

See [README.md](README.md) and `generate_lliuwin_img.sh`. The former LliureX-specific generator options are replaced by a single new-output-directory argument. The build requires root on Ubuntu 24.04 amd64 and refuses to reuse an existing output directory.

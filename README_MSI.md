# MSI build

Run `./generate_msi.sh` after `make build`, with Ubuntu's `wixl` installed. The MSI packages the launcher; it does not silently install or remove Ubuntu. Use the separate Ubuntu uninstall entry for the disk and boot entry.
